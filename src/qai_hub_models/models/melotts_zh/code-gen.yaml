supported_precisions:
- mixed_with_float
disabled_paths:
  mixed_with_float:
    qnn_context_binary:
      scorecard_failure: 'Failure due to conversion to QNN context binary (Issue: 18251)'
is_collection_model: true
requires_aot_prepare: true
global_requirements_incompatible: true
pip_pre_build_reqs: --no-deps git+https://github.com/myshell-ai/MeloTTS.git
readme_install_system_deps: |
  #### Linux
  ```bash
  sudo apt install libportaudio2
  ```
