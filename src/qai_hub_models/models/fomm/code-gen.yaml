disabled_paths:
  float:
    tflite:
      scorecard_failure: 'Profiling Failure(s): generator:jpv137m7p'
    qnn_dlc:
      scorecard_failure: 'Profiling Failure(s): generator:jgklz8ky5'
is_collection_model: true
readme_install_system_deps: |
  #### Linux
  ```bash
  sudo apt install ffmpeg
  ```

  #### Windows
  ```
  winget install ffmpeg
  ```
