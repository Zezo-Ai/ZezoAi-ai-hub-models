disabled_paths:
  float:
    tflite:
      scorecard_failure: 'Compilation Failure(s): generator:jpx73161g'
    qnn_dlc:
      scorecard_failure: 'Profiling Failure(s): generator:jp836jmzg'
is_collection_model: true
readme_install_system_deps: |
  #### Linux
  ```bash
  sudo apt install ffmpeg
  ```

  #### Windows
  ```
  winget install ffmpeg
  ```
