disabled_paths:
  float:
    tflite:
      scorecard_failure: 'Profiling Failure(s): generator:jpr1vr29g'
    qnn_dlc:
      scorecard_failure: 'Profiling Failure(s): generator:jp1q49e8g'
is_collection_model: true
readme_install_system_deps: |
  #### Linux
  ```bash
  sudo apt install ffmpeg
  ```

  #### Windows
  ```
  winget install ffmpeg
  ```
