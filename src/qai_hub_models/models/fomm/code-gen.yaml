disabled_paths:
  float:
    tflite:
      scorecard_failure: 'Profiling Failure(s): FOMMGenerator:jgl0m1oeg'
    qnn_dlc:
      scorecard_failure: 'Profiling Failure(s): FOMMGenerator:jp27yvvx5'
is_collection_model: true
readme_install_system_deps: |
  #### Linux
  ```bash
  sudo apt install ffmpeg
  ```

  #### Windows
  ```
  winget install ffmpeg
  ```
