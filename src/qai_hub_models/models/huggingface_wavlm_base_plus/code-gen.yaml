has_on_target_demo: true
disabled_paths:
  float:
    qnn_dlc:
      scorecard_failure: 'Profiling Failure(s): j57jro1r5'
    onnx:
      scorecard_failure: 'Profiling Failure(s): j5634o975'
global_requirements_incompatible: true
readme_install_system_deps: |
  #### Linux
  ```bash
  sudo apt install ffmpeg
  ```

  #### Windows
  ```
  winget install ffmpeg
  ```
