has_on_target_demo: true
disabled_paths:
  float:
    tflite:
      scorecard_failure: 'Low on-device PSNR (12.29 dB) — relative position bias uses int64 embedding indices incompatible with TFLite (inference job j5wdv3dzg)'
global_requirements_incompatible: true
readme_install_system_deps: |
  #### Linux
  ```bash
  sudo apt install ffmpeg
  ```

  #### Windows
  ```
  winget install ffmpeg
  ```
