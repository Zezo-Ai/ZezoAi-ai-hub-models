numpy<=2.4.4
torchaudio>=2.4.0,<2.10.0
soundfile>=0.12.1
scipy>=1.7.0
jiwer>=3.0.0
omegaconf>=2.1.0
# The following packages are imported at module level by deepspeech_pytorch
# (directly or transitively) and must be listed explicitly here because
# global_requirements_incompatible: true means the global environment is
# not available in isolated installs. setuptools is pinned to <82 because
# deepspeech_pytorch uses pkg_resources, which was removed in setuptools 82.
pytorch-lightning==1.2.10
setuptools<82.0.0
torchmetrics>=0.2.0,<0.6.0
Levenshtein>=0.12.0,<1.0.0
hydra-core>=1.1.0
google-cloud-storage>=1.13.0
